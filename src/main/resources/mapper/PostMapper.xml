<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wait.mapper.PostMapper">

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO post (user_id, content, like_count, comment_count)
        VALUES (#{userId}, #{content}, #{likeCount}, #{commentCount})
    </insert>

    <select id="selectById" resultType="com.wait.entity.domain.Post">
        SELECT * FROM post WHERE id = #{id} AND is_deleted = 0
    </select>

    <select id="selectByUserId" resultType="com.wait.entity.domain.Post">
        SELECT p.*, u.username
        FROM post p
        LEFT JOIN user_base u ON p.user_id = u.id
        WHERE p.user_id = #{userId} AND p.is_deleted = 0
        ORDER BY p.id DESC
    </select>

    <!-- 分页查询用户帖子（数据库层面分页，性能更好） -->
    <select id="selectByUserIdWithPagination" resultType="com.wait.entity.domain.Post">
        SELECT p.*, u.username
        FROM post p
        LEFT JOIN user_base u ON p.user_id = u.id
        WHERE p.user_id = #{userId} AND p.is_deleted = 0
        ORDER BY p.id DESC
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <select id="selectByIds" resultType="com.wait.entity.domain.Post">
        SELECT p.*, u.username
        FROM post p
        LEFT JOIN user_base u ON p.user_id = u.id
        WHERE p.id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
        AND p.is_deleted = 0
        ORDER BY p.id DESC
    </select>

    <update id="update">
        UPDATE post
        SET content = #{content}, like_count = #{likeCount}, comment_count = #{commentCount}
        WHERE id = #{id}
    </update>

    <update id="delete">
        UPDATE post SET is_deleted = 1 WHERE id = #{id}
    </update>

    <select id="countByUserId" resultType="int">
        SELECT COUNT(*) FROM post WHERE user_id = #{userId} AND is_deleted = 0
    </select>

</mapper>
